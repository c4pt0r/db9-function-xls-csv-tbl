{
  "name": "db9-function-xls-csv-tbl",
  "version": "1.0.0",
  "description": "db9 function: convert xlsx files to CSV via fs9",
  "scripts": {
    "build": "esbuild src/index-fs9.ts --bundle --platform=node --target=node18 --format=cjs --outfile=dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.0.0"
  }
}
